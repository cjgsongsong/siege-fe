/** Language codes. @enum */
export const LANGUAGE_CODES = { ENGLISH: "en" } as const;

/** Internationalization constants. @enum */
export const INTERNATIONALIZATION = {
  FALLBACK_LANGUAGE: LANGUAGE_CODES.ENGLISH,
} as const;
