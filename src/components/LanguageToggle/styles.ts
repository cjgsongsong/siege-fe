import styled from "styled-components";

/** Styled toggle. @component */
export default styled.div`
  cursor: pointer;
  display: fixed;
  font-size: 36px;
  padding-right: 10px;
  position: absolute;
  right: 0;
`;
